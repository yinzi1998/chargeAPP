<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chargeapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChargeMonthItemActivity"></activity>
        <activity android:name=".ChargeMonthListActivity" />
        <activity android:name=".ChargeDayItemActivity" />
        <activity android:name=".ChargeDayListActivity" />
        <activity android:name=".ChargeDetailItemActivity" />
        <activity android:name=".ChargeDetailListActivity" />
        <activity android:name=".WriteChargeActivity" />
        <activity android:name=".IndexActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>